version: "3.9"

services:
  postgres:
    image: postgres:16-alpine
    container_name: classroom-rental-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DB_NAME:-classroom_rental}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
    ports:
      - "${PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres} -d ${DB_NAME:-classroom_rental}"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
