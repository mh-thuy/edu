# Tuition list

Route `/admin/tuition-fees`, API `GET /api/tuition-fees`. Có lọc mã học sinh/trạng thái, loading/error/empty state, bảng tổng tiền, đã thu, còn nợ, hạn thanh toán và `StatusBadge`. Hiện nút thanh toán với `UNPAID`/`PARTIAL`/`OVERDUE`.
