# Tuition list

Route `/admin/tuition-fees`, API `GET /api/tuition-fees`. Có lọc mã học sinh/trạng thái, loading/error/empty state, bảng tổng tiền, hạn thanh toán và `StatusBadge`. Chỉ hiện nút thanh toán với `UNPAID`/`OVERDUE`; không có cột paid/outstanding/partial.
